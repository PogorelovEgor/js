let role = "Admin";

switch(role){
    case "Manager": {
        console.log(`Manager in ${role}`);
        break;
    }
    case "Admin": {
        console.log(`Manager in ${role}`);
        break;
    }
    case "User": {
        console.log(`Manager in ${role}`);
        break;
    }
    default: {
        console.log("Access denied");
    }
}